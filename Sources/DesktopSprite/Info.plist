<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>1.0</string>
	<key>CFBundleVersion</key>
	<string>1</string>

	<!--
		Runs the app as a background agent: no Dock icon, no application menu bar,
		and it never becomes the active app. This is what makes it feel like part of
		the desktop rather than a window you have to manage.

		Consequence: there is no File > Quit. The MenuBarExtra in DesktopSpriteApp.swift
		is the only way to quit, so do not remove it.
	-->
	<key>LSUIElement</key>
	<true/>

	<!--
		LSMinimumSystemVersion is deliberately absent. Xcode injects it from the
		target's MACOSX_DEPLOYMENT_TARGET build setting at build time; hardcoding it
		here means two sources of truth that warn whenever they disagree.

		Set the minimum version in Build Settings ▸ macOS Deployment Target.
		The code requires 13.0 or later.
	-->
	<key>NSHumanReadableCopyright</key>
	<string></string>
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>
</dict>
</plist>
